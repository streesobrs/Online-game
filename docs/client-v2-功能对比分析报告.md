# v1 / v2 客户端功能对比分析报告

> 分析日期：2026-08-18
> 对比对象：`client/`（v1 多页应用，index.html 12672 行 + 4 个子页面） vs `client-v2/`（v2 模块化 SPA，ES Modules 无构建）
> 分析方法：全量代码走读 + Socket 事件映射比对（v1 共 70 个 `socket.on` / 69 个 `socket.emit`）+ 任务清单交叉验证

---

## 一、总体结论

| 维度 | 结论 |
|------|------|
| 整体完成度 | **约 95%**。任务清单显示阶段 1-4 全部完成（72/72），阶段 5 进行中（6/7） |
| 协议覆盖 | v2 `core/socket.js` EVENT_MAP 覆盖 v1 全部 70 个事件，另新增私聊/反馈/对局重连 3 组事件 |
| 核心游戏功能 | 4 棋种联机、AI 对战、观战、贪吃蛇单人/双人/道具 **全部对齐** |
| 主要缺口 | 集中在**体验层**：音效、游戏规则弹窗、自定义快捷键、排行榜搜索、落子记录/对局信息面板、导出对局数据 |
| v2 反超项 | 私聊系统、对局重连恢复、邮件完整 UI、等级奖励领取、交易记录、模块懒加载 |

**状态标注说明**：✅ 已对齐 ｜ ⚠️ 部分实现 ｜ ❌ 完全缺失 ｜ 🌟 v2 更优

---

## 二、功能对比总清单

### 2.1 核心功能（游戏对局）

| # | 功能点 | v1 实现 | v2 实现 | v2 状态 |
|---|--------|---------|---------|---------|
| 1 | 五子棋联机对战（15×15、落子同步、胜负判定、长连算胜） | ✅ | `games/gobang/`（board 162 + rules 83 + play 327 行） | ✅ |
| 2 | 围棋联机对战（19×19、提子、自杀规则禁手） | ✅ | `games/go/`（board 181 + rules 170 + play 374 行） | ✅ |
| 3 | 象棋联机对战（走子规则/蹩马腿/塞象眼/将军判定） | ✅ | `games/chinese-chess/`（rules 235 + play 453 行） | ✅ |
| 4 | 贪吃蛇单人模式（本地高分 + `snake_sync_highscore` 同步） | ✅ | `snake/index.js` L95-L109 | ✅ |
| 5 | 贪吃蛇联机双人（`snake_match_found` 专属协议、无限复活） | ✅ | `snake/play.js` 全 351 行 | ✅ |
| 6 | 贪吃蛇道具×4（复活卡 5s 无敌/加速/双倍/缩短，双击使用） | ✅ | `snake/index.js` L36-L39、L170-L332 | ✅ |
| 7 | 贪吃蛇虚拟按键（移动端方向键） | ✅ | `snake/styles.css` `.snake-virtual-btn` | ✅ |
| 8 | 悬停预览、最后落子高亮 | ✅ | `gobang/board.js` L51、`go/board.js` L55 | ✅ |
| 9 | 象棋选中高亮、可走位置提示 | ✅ | `chinese-chess/styles.css` `.valid-move` | ✅ |
| 10 | 悔棋（直接次数 + 悔棋卡折算 3 次，自动消耗重试） | ✅ | `games/board-actions.js` L50-L65 | ✅ |
| 11 | 提示（提示卡折算 5 次，棋盘高亮推荐位，象棋含起终点） | ✅ | `board-actions.js` L71-L129 | ✅ |
| 12 | 认输、求和重置（请求/确认/拒绝/超时） | ✅ | 各棋种 `play.js`（resetRequest 全流程） | ✅ |
| 13 | AI 对战（3 棋种 × 3 难度） | ✅ | `ai-battle/play.js` 355 行 + 统一大厅难度卡片 | ✅ |
| 14 | 观战（列表/加入/实时同步/退出，蛇不可观战与 v1 一致） | ✅ | `spectate/index.js` + `play.js` | ✅ |
| 15 | 对局计时器 | ✅ | 各棋种 `play.js` | ✅ |
| 16 | 对局重连恢复（刷新后"继续/放弃"面板 + 兜底查询 + 断线补发） | ❌ 无 | `games/index.js` L242-L435 | 🌟 v2 新增 |
| 17 | 落子记录面板（左侧实时棋谱） | ✅ `#move-log` | 无 | ❌ |
| 18 | 三大对局信息面板（联机状态/网络延迟/落子总数/连子数/对局时长/落子频率/鼠标坐标/空位数等 18 项） | ✅ L3988-L4076 | 仅对手信息+回合+计时 | ⚠️ 大幅简化 |
| 19 | 高级统计面板（棋盘占比/黑白胜率等） | ✅ `toggleAdvancedStats` L4402 | 无 | ❌ |
| 20 | 导出对局数据 | ✅ `exportGameData` L4082 | 无 | ❌ |
| 21 | 游戏规则弹窗（4 棋种规则文本，'t' 键 + 按钮） | ✅ `showGameTips` L10917 | 仅贪吃蛇有规则文本 | ❌（棋类缺失） |

### 2.2 次要功能（社交/系统）

| # | 功能点 | v1 实现 | v2 实现 | v2 状态 |
|---|--------|---------|---------|---------|
| 22 | 登录/注册/游客登录/重置密码（Socket 协议，token 与 v1 互通） | ✅ | `auth/login.js` + `core/auth.js` | ✅ |
| 23 | 全局聊天 + 局内聊天双频道 | ✅ | `chat/index.js` 453 行 | ✅ |
| 24 | 私聊（会话列表/未读提醒/离线消息） | ❌ 无 | `chat/index.js` L108-L172 | 🌟 v2 新增 |
| 25 | 在线玩家列表 + 挑战（请求/接受/拒绝弹窗） | ✅ | `online-players/index.js` L88-L120 | ✅ |
| 26 | 排行榜（类型筛选 5 项 + Top20 + 我的排名） | ✅ | `leaderboard/index.js` | ✅ |
| 27 | 排行榜玩家搜索 + 参与人数统计 | ✅ `filterLeaderboard` L4163 | 无 | ❌ |
| 28 | 成就系统（解锁推送/列表/进度/分类/徽章数） | ✅ | `achievements/index.js` 217 行 + profile tab | ✅ |
| 29 | 商城（商品/购买/余额/VIP 对比表/背包） | ✅ | `shop/index.js` 512 行 | ✅ |
| 30 | 外观系统（拥有列表 + 装备） | ✅ shop.html | `api.shop.equipCosmetic`，profile L147 调用 | ✅ |
| 31 | 反馈（提交/列表/投票/评论/楼中楼回复/点赞/删除） | ✅ | `feedback/index.js` 320 行（含状态标签 4 种） | ✅ |
| 32 | 对战历史 + 回放（播放/暂停/速度/进度跳转） | ✅ 双页面 1247 行 | `replay/index.js` 506 行一体化 | ✅ |
| 33 | 个人资料（头像上传/改名/密码/战绩/经验等级） | ✅ | `profile/index.js` 1023 行 | ✅ |
| 34 | 邮件系统 | 仅 toast 通知（L7155） | 完整 UI（列表/领取/一键领取/已读/删除）profile L593-L740 | 🌟 v2 更优 |
| 35 | 等级奖励领取 + 星钻/经验交易记录 | ❌ 无 | profile L491-L547、L442-L443 | 🌟 v2 新增 |
| 36 | 主题切换（4 主题 + 预览卡片 + 持久化） | ✅ 服务端动态列表（`/api/themes`） | `themes/index.js` 硬编码本地 4 主题 | ⚠️ 未接服务端动态列表 |
| 37 | 维护横幅/断连重连提示/版本检查/广播/管理员消息 | ✅ | `core/system.js` 281 行全覆盖 | ✅ |
| 38 | 主题 CSS 加载性能（缓存 CSS 文本，切换零闪烁） | ❌ link 切换 | `themes/index.js` `cssCache` | 🌟 v2 更优 |

### 2.3 辅助功能（操作/体验）

| # | 功能点 | v1 实现 | v2 实现 | v2 状态 |
|---|--------|---------|---------|---------|
| 39 | 导航快捷键（切视图/游戏） | ✅ | `utils/shortcut.js` 9 键 + 游戏页内 6 键 | ✅ |
| 40 | 游戏操作快捷键（z 悔棋/u 认输/n 新局/p 暂停/m 匹配/r 返回/Shift+Z 重做） | ✅ L4764-L4770 | 无 | ❌ |
| 41 | 自定义快捷键（改键 UI/持久化/重置/帮助面板 '?'） | ✅ L4784-L4831 + L5030 | 无 | ❌ |
| 42 | 音效系统（AudioContext 合成 5 种：落子/吃子/胜利/错误/点击） | ✅ L5509-L5549 | 无（全库 0 匹配） | ❌ |
| 43 | 音效开关（'s' 键切换 + 状态提示） | ✅ L4892 | 无 | ❌ |
| 44 | 移动端响应式适配 | ✅ 大量媒体查询 + touch 优化 | `styles/responsive.css` 124 行 + `utils/responsive.js` | ✅（阶段 5.1 已验收 2/2） |
| 45 | 性能优化（模块懒加载 dynamic import） | ❌ 单文件全量 | `games/index.js` 按需 import 各棋种 | 🌟 v2 更优 |
| 46 | v1 → v2 切换入口 | ✅ 导航栏「✨ 新版」按钮 | 无返回 v1 入口 | ⚠️（可选） |

---

## 三、完全缺失项清单（v1 有 → v2 无）

| 编号 | 缺失功能 | v1 证据位置 | 影响 | 补齐难度 |
|------|---------|------------|------|---------|
| M1 | **音效系统**（落子/吃子/胜利/错误/点击 5 种 + 开关） | index.html L5509-L5549 | 对局打击感、操作反馈明显减弱 | 低（合成音无资源文件，v1 逻辑可直接移植） |
| M2 | **游戏规则弹窗**（五子棋/围棋/象棋规则说明） | index.html L10917 `showGameTips` + 按钮 L4087 | 新用户无从了解规则，上手门槛升高 | 低（静态文本 + modal） |
| M3 | **自定义快捷键系统**（改键/持久化/重置/帮助面板） | index.html L4784-L4831、L5030 | 键盘党操作效率下降 | 中（v1 有完整参考实现） |
| M4 | **游戏操作快捷键**（z 悔棋/u 认输/n 新局/p 暂停/m 匹配/r 返回/Shift+Z 重做） | index.html L4764-L4777 | 高频操作需鼠标点击，便捷性下降 | 低（接入 v2 现有函数即可） |
| M5 | **排行榜玩家搜索 + 参与人数** | index.html L4161-L4167 | 大榜找人不便 | 低（前端过滤 + 已有 `api.users.search`） |
| M6 | **落子记录面板**（实时棋谱） | index.html L4110-L4116 | 观战/复盘时无法回顾行棋顺序 | 中（需各棋种 play.js 埋点） |
| M7 | **导出对局数据** | index.html L4082 `exportGameData` | 无法保存/分享对局 | 低（JSON 下载） |
| M8 | **高级统计面板** | index.html L4402-L4419+ | 数据向用户缺少分析视图 | 低-中 |

## 四、部分实现 / 存在差距项

| 编号 | 功能 | 差距描述 | 改进方向 |
|------|------|---------|---------|
| P1 | **对局信息面板** | v1 有 18 项实时指标（网络延迟/对局时长/落子总数/连子数/落子频率/鼠标坐标/空位数等）；v2 仅对手信息+回合+计时器 | 分两档补齐：先加**网络延迟 + 对局时长 + 落子总数**（数据在 play.js 已存在，纯 UI）；其余低频指标可并入高级统计 |
| P2 | **主题系统** | v2 硬编码 4 个本地主题，`api.themes.list()` 已封装但未使用；服务端 ThemeManager 上传的新主题 v2 不可见 | 切换到 `/api/themes` 动态列表，本地 4 主题作兜底；预览卡片样式沿用 |
| P3 | **快捷键体系** | 仅导航键 9 个 + 游戏页内 6 个；无帮助面板（'?'）、无自定义 | 先补帮助面板与游戏操作键（M3/M4），再上自定义 |
| P4 | **贪吃蛇对局恢复** | v2 重连面板明确提示"贪吃蛇对局暂不支持中途恢复"（`games/index.js` L286） | 可接受（实时游戏快照成本高），建议保留提示并允许直接放弃 |
| P5 | **阶段 5 收尾** | 任务清单 5.2 性能优化 1/2、5.3.2 最终验收未完成 | 按 5.x 任务顺序收尾后再启动新增优化 |

## 五、v2 反超 v1 的项（保持并宣传）

1. **私聊系统**：会话列表/未读提醒/离线消息/在线玩家私聊入口（v1 完全没有）
2. **对局重连恢复**：战局内刷新不丢局，"继续/放弃"面板 + 三重兜底（快照暂存 store、connect 后主动查询、断线期间标记待放弃重连补发）
3. **邮件完整 UI**：列表/单封领取/一键领取/已读/删除（v1 仅 toast）
4. **等级奖励 + 交易记录**：profile 页直接领取等级奖励、查看星钻/经验流水
5. **性能架构**：模块懒加载（游戏模块 dynamic import）、主题 CSS 文本缓存（切换零闪烁）、单文件 ≤ 300 行规范
6. **聊天体验**：全局历史加载、连接后自动补拉
7. **统一游戏大厅**：联机/AI 双模式 Tab + 侧边栏选游戏，导航不再拥挤

---

## 六、优化建议清单（按维度）

### 6.1 用户体验维度
- U1 补齐音效系统（M1），并加设置开关（音量/静音，存 localStorage）
- U2 补游戏规则弹窗（M2）：统一大厅侧边栏加"📜 规则"入口，文案直接复制 v1
- U3 对局结束增加"🔍 查看回放"直达按钮（回放模块已有，仅缺入口联动）
- U4 排行榜搜索（M5）：输入即过滤 + 高亮命中项
- U5 v1/v2 互切入口：v2 顶栏加「返回旧版」，降低灰度期用户流失

### 6.2 操作便捷性维度
- O1 游戏操作快捷键（M4）：z 悔棋 / u 认输 / n 新局 / m 匹配 / Esc 取消（Enter 匹配 v2 已有）
- O2 快捷键帮助面板（'?' 或 K 键）：展示当前全部键位（含页内键），为自定义快捷键铺路
- O3 自定义快捷键（M3）：移植 v1 改键 UI + localStorage 持久化，仅开放导航键与游戏操作键
- O4 落子记录面板（M6）：对局侧栏可折叠列表，点击记录高亮对应棋子

### 6.3 性能/扩展性维度
- E1 主题动态化（P2）：接入 `/api/themes`，服务端主题即发即用
- E2 导出对局数据（M7）：JSON 棋谱下载，为后续导入复盘留接口
- E3 阶段 5.2 剩余性能项收尾（任务清单未完成项）
- E4 观战列表自动刷新（当前仅进入时拉取，可加 30s 轮询或 socket 推送）

### 6.4 健壮性维度（两版都缺的新增建议）
- R1 `beforeunload` 对局中关闭页面二次确认（两版都没有）
- R2 断线重连后自动补拉聊天未读（私聊已有离线消息，全局频道可对齐）
- R3 蛇观战支持（远期，需服务端配合，成本高收益低，可不做）

---

## 七、优先级排序（重要度 × 实施难度）

### P0 — 高价值低成本，建议立即实施
| 优先 | 事项 | 对应编号 | 预估规模 | 理由 |
|------|------|---------|---------|------|
| 1 | 游戏规则弹窗 | M2/U2 | 极小（静态文本+modal） | 新用户第一需求，零风险 |
| 2 | 音效系统 + 开关 | M1/U1 | 小（移植 v1 合成音逻辑） | 对局体验核心反馈 |
| 3 | 排行榜搜索 | M5/U4 | 极小（前端过滤） | 数据已有，纯 UI |
| 4 | 快捷键帮助面板 | O2 | 极小 | 与 O1 搭配发布 |

### P1 — 高价值中成本，第二批实施
| 优先 | 事项 | 对应编号 | 预估规模 | 理由 |
|------|------|---------|---------|------|
| 5 | 游戏操作快捷键 | M4/O1 | 小 | 依赖现有函数，接入即可 |
| 6 | 对局信息增强（延迟/时长/落子总数） | P1 | 小-中 | 数据已在，补展示 |
| 7 | 主题动态列表 | P2/E1 | 中 | api 已封装，打通即可 |
| 8 | 落子记录面板 | M6/O4 | 中 | 需各棋种埋点，观战同样受益 |
| 9 | 导出对局数据 | M7/E2 | 小 | 独立无依赖 |

### P2 — 中价值，第三批实施
| 优先 | 事项 | 对应编号 | 预估规模 |
|------|------|---------|---------|
| 10 | 自定义快捷键系统 | M3/O3 | 中 |
| 11 | 高级统计面板 | M8 | 小-中 |
| 12 | v1/v2 互切入口 | U5 | 极小 |
| 13 | 观战列表自动刷新 | E4 | 小 |

### P3 — 低优先 / 远期
| 优先 | 事项 | 对应编号 | 说明 |
|------|------|---------|------|
| 14 | beforeunload 对局保护 | R1 | 两版都缺，锦上添花 |
| 15 | 全局频道离线补拉 | R2 | 私聊已有，可对齐 |
| 16 | 蛇观战 | R3 | 需服务端改动，不建议（受"服务端不动"红线约束） |

### 建议实施节奏
```
第一批（P0）：规则弹窗 + 音效 + 榜单搜索 + 快捷键帮助   → 一周内可全部落地
第二批（P1）：对局快捷键 + 信息增强 + 主题动态化 + 落子记录 + 导出
第三批（P2）：自定义快捷键 + 高级统计 + 互切入口
收尾：完成任务清单阶段 5 剩余项（5.2 性能 1 项 + 5.3.2 最终验收）后，v2 可全面替代 v1
```

---

## 八、结论

v2 客户端在**功能对齐度上已达约 95%**，核心对战链路（匹配/对局/悔棋/提示/道具/AI/观战/回放）与 v1 完全对等且协议兼容，并在私聊、对局重连、邮件、等级奖励、性能架构上实现反超。

剩余缺口集中在**体验细节层**（音效/规则说明/快捷键/信息面板/榜单搜索），全部为纯前端改动、不触碰服务端红线，且多数有 v1 成熟实现可移植，补齐成本低。建议按 P0 → P1 → P2 顺序分三批补齐，并优先收尾任务清单阶段 5 的遗留项。
