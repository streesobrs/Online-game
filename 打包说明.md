# 打包说明

## 方式一：压缩包打包 (推荐)

### 使用方法：
1. 双击 `package.bat`
2. 等待打包完成
3. 在当前目录下会生成 `game-server-YYYYMMDD.zip`

### 包含内容：
- server/ 文件夹（包含modules、config、assets、utils等）
- index.html
- package.json
- start.bat
- start.ps1
- .gitignore (如果存在)

### 注意：
- `server/config/themes/` 主题配置文件会被保留
- `server/data/` 用户数据文件会被排除
- `server/logs/` 日志文件会被排除

---

## 方式二：EXE 可执行文件打包

### 前置准备：
1. 首次使用需要安装依赖：
   ```bash
   npm install
   ```

### 使用方法：
1. 双击 `build-exe.bat`
2. 等待打包完成
3. 在 `dist/` 文件夹下会生成 `game-server.exe`

### 使用打包后的 EXE：
1. 确保以下文件和 `game-server.exe` 在同一目录：
   - server/ 文件夹（包含modules、config、assets等）
   - index.html
2. 双击 `game-server.exe` 即可运行
3. 不需要安装 Node.js！

### 打包内容说明：
- ✅ server/modules/ - 所有功能模块
- ✅ server/config/ - 配置文件（包含主题配置）
- ✅ server/assets/ - 静态资源
- ✅ server/utils/ - 工具函数
- ❌ server/data/ - 用户数据（运行时生成）
- ❌ server/logs/ - 日志文件（运行时生成）

---

## 注意事项

### EXE 打包注意：
- 打包后的 EXE 体积较大（约 50MB+）
- 需要保留 server/ 文件夹和 index.html
- 首次运行可能需要防火墙放行

### 压缩包注意：
- 接收方需要安装 Node.js
- 接收方需要运行 `npm install` 安装依赖
- 然后运行 `start.bat` 启动

---

## 快速开始

### 开发者（有 Node.js）：
```bash
# 1. 解压压缩包
# 2. 安装依赖
npm install
# 3. 启动
start.bat
```

### 用户（无 Node.js）：
```
1. 确保 dist/game-server.exe 和 server/、index.html 在同一目录
2. 双击 game-server.exe
3. 访问 http://localhost:8080
```
